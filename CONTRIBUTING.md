# Contributing

`markdown-it-digit` へのコントリビューションありがとうございます。

このドキュメントでは、開発時の基本的なルールと手順を説明します。

## 開発環境

リポジトリを取得したあと、依存パッケージをインストールします。

```sh
npm install
```

利用可能な開発用コマンドについては、`package.json` の `scripts` を確認してください。

## ブランチ

開発では、用途に応じて以下のブランチを使用します。

### ブランチの命名規則

| ブランチ名       | 役割                      | 派生元       | マージ先              |
| ----------- | ----------------------- | --------- | ----------------- |
| `main`      | 公開するものを置くブランチ           | -         | -                 |
| `develop`   | 開発中のものを置くブランチ           | `main`    | -            |
| `release-*` | 次にリリースするものを置くブランチ       | `develop` | `develop`, `main` |
| `feature-*` | 新機能の開発に使用するブランチ         | `develop` | `develop`         |
| `hotfix-*`  | 公開中のバージョンのバグ修正に使用するブランチ | `main`    | `develop`, `main` |

### `main`

公開するバージョンのコードを管理するブランチです。

リリース可能な安定した状態を維持し、通常の機能追加や開発作業を `main` 上で直接行いません。

通常は以下のブランチから変更を受け入れます。

* `release-*`：通常のリリース
* `hotfix-*`：公開中のバージョンに対する緊急のバグ修正

リリース時には `release-*` を `main` にマージし、公開するバージョンを確定します。

公開中のバージョンに緊急の修正が必要な場合は、`main` から `hotfix-*` を派生させます。

`main` は継続して使用する基幹ブランチのため、削除しません。

### `develop`

次のリリースに向けた開発内容を統合するブランチです。

通常の機能追加は、`develop` から `feature-*` を派生させて行います。

完成した `feature-*` を `develop` にマージすることで、次回リリース予定の機能を集約します。

リリース対象となる機能が揃ったら、`develop` から `release-*` を派生させ、リリース準備へ移行します。

通常のリリースでは `develop` を直接 `main` にマージせず、`release-*` を経由して `main` に反映します。

また、`release-*` や `hotfix-*` で行われた修正も `develop` にマージし、以降の開発へ反映します。

`develop` は継続して使用する基幹ブランチのため、削除しません。

### `feature-*`

新機能の開発や、次回リリースに向けた個別の機能変更に使用するブランチです。

`develop` から派生させ、1つの `feature-*` では原則として1つの目的に関する変更を行います。

開発中は、実装だけでなく必要に応じて以下も行います。

* テストの追加・更新
* 関連するドキュメントの更新
* Lintによるコードの確認
* 既存機能への影響確認

開発と動作確認が完了したら `develop` にマージし、次回リリース予定の開発内容へ統合します。

ブランチ名には変更内容が分かる簡潔な名前を付けます。

例：

```text
feature-japanese-units
feature-comma-formatting
```

`develop` へのマージ完了後、不要になった `feature-*` ブランチは削除します。

### `release-*`

次のバージョンをリリースするための準備に使用するブランチです。

リリース対象となる機能の開発が完了し、必要な `feature-*` が `develop` にマージされた段階で、`develop` から `release-*` を作成します。

`release-*` では、原則として新しい機能の開発は行わず、リリースに向けた以下の作業を行います。

* リリース前の動作確認とテスト
* リリース前に発見されたバグの修正
* バージョン番号の更新
* `CHANGELOG.md` の更新
* `README.md` などのドキュメントの最終確認・修正
* `npm pack` などによる公開内容の確認
* その他、リリースに必要な調整

リリース準備中に修正した内容は、リリース後の開発にも反映されるようにします。

リリース準備が完了したら、`release-*` を `main` にマージして公開するバージョンを確定します。

その後、同じ `release-*` を `develop` にマージし、リリース準備中に行ったバグ修正や調整を今後の開発へ反映します。

ブランチ名には、原則としてリリース予定のバージョン番号を使用します。

```text
release-1.0.0
release-1.1.0
release-2.0.0
```

リリース完了後、不要になった `release-*` ブランチは削除します。

### `hotfix-*`

現在 `main` で公開されているバージョンに、緊急の修正が必要になった場合に使用するブランチです。

通常の開発を行っている `develop` ではなく、現在公開されているコードを基準に修正するため、`main` から派生させます。

`hotfix-*` では、原則として対象となる問題の修正だけを行い、関係のない機能追加や変更は行いません。

必要に応じて以下も行います。

* バグを再現するテストの追加
* 修正後のテスト
* バージョン番号の更新
* `CHANGELOG.md` の更新
* 公開内容の確認

修正が完了したら `main` にマージし、修正版をリリースします。

同時に `develop` にもマージし、同じ修正が今後の開発から失われないようにします。

ブランチ名には修正内容が分かる簡潔な名前、または修正版のバージョン番号を使用します。

例：

```text
hotfix-comma-formatting
hotfix-1.0.1
```

修正版のリリース完了後、不要になった `hotfix-*` ブランチは削除します。


### 基本的な開発フロー

通常の新機能開発は、以下の流れで行います。


```text
main
  │
  └── develop
        │
        ├── feature-a
        │      ↓
        │   develop
        │
        ├── feature-b
        │      ↓
        │   develop
        │
        │
        └── release-1.0.0
               │
               ├─ バグ修正
               ├─ バージョン調整
               ├─ CHANGELOG更新
               └─ リリース前確認
                       ↓
                  main + develop
```

公開中のバージョンに緊急の修正が必要な場合は、`main` から `hotfix-*` を作成します。

```text
main
  │
  └── hotfix-xxx
          ↓
     main + develop
```

ブランチを作成する前に、目的に応じた派生元ブランチにいることを確認してください。



## 実装

実装前に関連するコードと既存仕様を確認し、変更による影響範囲を把握してください。

実装時は以下の方針に従います。

* 既存のプロジェクト構成に従う
* 既存のコーディングスタイルに従う
* 変更は目的を達成するために必要な最小限の範囲にとどめる
* 変更と関係のないファイルやコードを変更しない
* 必要がない限り、新しい依存パッケージを追加しない
* 既存の公開APIや動作との互換性を考慮する

仕様として明示されていない事項について判断が必要になった場合は、既存仕様や既存コードとの整合性を確認してください。

ユーザーから見た動作、公開API、互換性などに影響する判断については、実装前に方針を明確にしてください。

内部実装上の軽微な判断については、既存コードと既存の設計方針に従います。

## テスト

テストコードは原則として `test/` 以下に配置します。

機能追加や仕様変更を行った場合は、その動作を確認できるテストを追加または更新してください。

バグ修正では、可能な限りバグを再現するテストを追加し、同じ問題が再発した場合に検出できるようにしてください。

テストを通すことだけを目的として、正しい既存テストの期待値や条件を変更しないでください。

実装後は、`package.json` に定義されているテストスクリプトを実行します。

通常は以下を使用します。

```sh
npm test
```

すべてのテストが成功することを確認してください。

## Lint

実装後は、`package.json` に定義されているLintスクリプトを実行します。

通常は以下を使用します。

```sh
npm run lint
```

変更によって発生したLintエラーがないことを確認してください。

## コミット

コミットする前に変更差分を確認し、意図しない変更が含まれていないことを確認してください。

```sh
git status
git diff
```

複数の無関係な変更を1つのコミットにまとめず、変更の目的ごとに適切な単位でコミットしてください。

### コミットメッセージ

コミットメッセージは、`.gitmessage` に定義された形式とルールに従って作成してください。

コミット前に `.gitmessage` の内容を確認し、実際の変更差分を正確に反映したコミットメッセージを作成してください。

コミットメッセージの形式、変更種別、記載方法については `.gitmessage` を正式なルールとし、このドキュメントには重複して記載しません。


## Pull Request

Pull Requestは、原則として1つの目的に対して作成してください。

Pull Requestには必要に応じて以下を記載します。

* 何を変更したか
* なぜ変更したか
* 実装方法や重要な設計判断
* 実行したテスト
* 既存機能や互換性への影響
* 関連するIssue

レビューしやすいよう、目的と関係のない変更を含めないでください。

## ドキュメント

ユーザーから見た動作や使用方法を変更した場合は、必要に応じて `README.md` などの関連ドキュメントも更新してください。

コードとドキュメントの内容が一致していることを確認してください。

## 依存パッケージ

新しい依存パッケージは、既存の機能や標準APIでは対応できない場合など、必要性が明確な場合にのみ追加してください。

依存パッケージを追加・更新する場合は、以下を確認してください。

* 追加する必要性
* メンテナンス状況
* ライセンス
* セキュリティ上の問題
* パッケージサイズや既存環境への影響

## バージョン

通常のリリースに伴うバージョン番号の変更は `release-*` で行います。

公開中のバージョンに対する緊急修正のバージョン番号変更は `hotfix-*` で行います。

通常の `feature-*` や `develop` では、リリースを目的としたバージョン番号の変更は行いません。

## 公開

`npm publish` は通常の開発作業では実行しません。

`npm publish` は、リリース作業として明示的に指示された場合のみ実行します。

公開前には、テストとLintを実行し、パッケージに含まれるファイルを確認します。

```sh
npm test
npm run lint
npm pack --dry-run
```

すべての確認が完了し、意図しないファイルがnpmパッケージに含まれていないことを確認してから公開します。
