# AGENTS.md

## 基本方針

- ユーザーとのやり取りは日本語で行う
- 既存のプロジェクト構成とコーディングスタイルに従う
- 実装前に関連コードと既存仕様を確認し、影響範囲を把握する
- 依頼を達成するために必要な最小限の変更にとどめる
- 依頼と関係のないファイルやコードは変更しない
- 必要がない限り、新しい依存パッケージを追加しない
- 開発手順、ブランチ運用、テスト、Lint、コミット、公開については `CONTRIBUTING.md` に従う
- 仕様として明示されていない事項のうち、ユーザーから見た動作、公開API、互換性などに影響する判断が必要な場合は、勝手に決定せず実装前に方針を提示する
- 内部実装上の軽微な判断は、既存コードや既存の設計方針に従って決定してよい
- 実装後は `package.json` に定義されているテストと lint を実行し、結果を確認する
- 作業中に依頼と無関係な問題を発見した場合は、勝手に修正せず報告する



### 開発ルール

- src以下に実装する
- test以下にテストを書く
- 既存APIとの互換性を維持する

### 作業前

コードを変更する前に、以下を確認する。

1. 関係するファイルを確認する
2. `package.json` を確認する
3. 既存の実装を確認する

ソースコードから確認できることを推測で判断しない。

### テスト

- テストコードは原則として `test/` 以下に配置する
- 機能追加や仕様変更では、必要に応じて対応するテストを追加・更新する
- バグ修正では、可能な限り再発防止テストを追加する
- 既存テストを通すためだけにテスト内容を変更しない
- テストは `package.json` に定義されたスクリプトを使用して実行する

### npm

- `npm publish` は明示的に指示された場合のみ実行する
- バージョン番号は明示的に指示された場合のみ変更する
- 公開に関係する変更では `npm pack --dry-run` で公開内容を確認する

### Git

- 明示的に指示されない限り、以下は実行しない。
  - `git commit`
  - `git push`
  - `git reset --hard`
  - force push
  - ブランチの削除

- 「コミットメッセージを作成して」と頼まれたら、変更差分と `.gitmessage` の内容を確認し、その形式に従って今回の変更内容を正確に反映したコミットメッセージを提出する

以下の確認用コマンドは必要に応じて使用してよい。

- `git status`
- `git diff`
- `git log`

### 変更範囲

依頼された作業に必要なファイルだけを変更する。

作業中に依頼と関係のない問題を発見した場合は、
勝手に修正せずユーザーに報告する。

### ファイル検索

この環境では、ファイルやコードの調査に以下のコマンドを使用できる。

- `rg`: ripgrepによる高速検索
- `findpath`: `rg` を検索エンジンとして使用する、ファイルやフォルダを検索するための自作PowerShellコマンド
- `findfiles`: `rg` を検索エンジンとして使用する、ファイル内容を検索するための自作PowerShellコマンド

`findpath` と `findfiles` の詳細な使用方法が必要な場合は、各コマンドの `-Help` を確認する。


## プロジェクト概要

`markdown-it-digit` は、Markdown内の数値表記を読みやすく整形するための markdown-it プラグイン。

主な機能は以下。

- 数値に3桁区切りのカンマを自動的に付与する
- 日本語の数値単位（万・億など）を自動的に付与する
- `$数値${設定}`でレンダリングすると、1,000,000や100~万~0000となる

## 仕様

数値変換の詳細な仕様については `docs/specification.md` を参照する。
実装を変更する場合は、既存仕様との互換性を確認する。
